# 商榷事项与执行口径

日期：2026-07-07

## 已按当前口径执行

1. **旧 results 不删除、不覆盖。**
   - 原目录如 `/root/project/results/phase2_v2`、`/root/project/results/mid` 等全部保留。
   - 新一轮 FlashAttention + Liger 重训输出统一放在：
     - `/root/project/results/refactor_flash_liger_20260707/`

2. **训练后清理大权重的范围。**
   - 只清理本轮新输出目录里的大文件，例如 `*.safetensors`、`*.pt`、optimizer/checkpoint 等。
   - 不清理旧 results。
   - 对有依赖的权重延迟清理：例如 phase2_v2 的英文 donor adapter 要等 MID/DSCT 训练和评估完成后再删。

3. **评估数据集。**
   - English: `mmlu`, `hellaswag`, `arc_challenge`, `truthfulqa_mc1`
   - Multilingual: `belebele` 的 `en,yo,so,ha`
   - IrokoBench: `yo,ha` 的 MCQ、AfriXNLI、AfriMGSM
   - 为贴合行动规划，新增 `scripts/evaluate.py --skip_sib200`，本轮 runner 默认跳过 SIB-200/FLORES。

4. **Layerwise 实验。**
   - 已重构 `scripts/train_layerwise.py`。
   - 原行动规划的脚本清单包含 layerwise，但结果目录清单没有列 `/root/project/results/layerwise`。
   - 本轮 runner 先执行明确列出的 7 类结果目录；保留 `RUN_LAYERWISE=1` 入口，若需要可追加跑 layerwise。

5. **GitHub。**
   - 小文件已同步至 `github.com:wozuicai/essay.git`。
   - 当前最近提交：`fe3462a Refactor training scripts for flash attention and liger`。

## 可能需要后续确认

1. 是否要把 Layerwise 也作为第 8 个完整 train/eval 实验跑完？
2. 是否要把本轮新结果最终复制/合并回原始目录名，还是保持 timestamp 子目录以便对比旧结果？当前默认保持 timestamp 子目录。
3. 训练耗时较长；如果 worker 时间有限，是否优先保留 phase2_v2/mix_en/tag_routing/MID/DSCT，还是优先 MoE/SSO？当前 runner 按依赖顺序自动执行。
