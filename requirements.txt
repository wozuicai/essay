# Core ML
torch>=2.7.1
torchvision
torchaudio
transformers>=4.57.0,<5.0.0
peft>=0.17.1,<0.18.0
accelerate>=1.0.0
deepspeed>=0.14.4
trl>=0.24.0,<0.25.0
liger-kernel
safetensors
tqdm

# Evaluation
lm-eval                # EleutherAI harness (MMLU, HellaSwag, ARC, TruthfulQA, SIB-200, Belebele)
sacrebleu              # FLORES BLEU / chrF
unbabel-comet          # COMET translation quality
langdetect             # LCB language confusion detection
fasttext               # GlotLID / LCB language ID
huggingface_hub
openai                 # Azure/OpenAI judge client used by eval_extended.py

# Data
datasets>=4.5.0,<5.0.0
sentencepiece
tokenizers>=0.22.2

# Claude judge (MT-Bench)
anthropic

# Experiment management
wandb
omegaconf

# Analysis
pandas
matplotlib
seaborn
scipy
